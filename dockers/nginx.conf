server {
    listen 80;
    server_name _;

    location /api/ {
        proxy_pass http://api:80/;
        proxy_set_header Host $host;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}